# UI conventions

- Buttons that add an item use the same compact size, variant, icon treatment, and spacing as the existing **Add Merit** action.
- Buttons that remove an item use the same compact card-level **Remove** treatment already used elsewhere.
- Feature-specific Add or Remove actions reuse these patterns instead of introducing a new width or scale.
